# Gabarits de l’instance

Un dossier par format × rendu (`F02-carrousel/`…), un fichier par compte (`<compte>.md`), créé depuis l’onglet Formats du tableau de bord ou par la routine `gabarits-quotidien`. Cadre et modèle : `README.md` et `_TEMPLATE_GABARIT.md` (framework).
