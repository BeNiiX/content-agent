---
name: screen-records-catalogue
description: Enregistrements d'écran de l'app classés par thème — source des extraits vidéo et des {{ITEM_PLURAL}} avec résultat (items.json)
type: assets
updated: {{DATE}}
---

# Enregistrements d'écran (par thème)

<!-- Comment remplir : un dossier par thème (`<thème>/<thème>-<date>-<clé>.mov`), une ligne par fichier. Après chaque ajout : OCR des cartes → `../items.json` (texte, résultat, `from` / `to`). -->

| Dossier / fichier | Date | {{ITEM_PLURAL}} | ICP | Notes |
|---|---|---|---|---|
| | | | | |

Toutes les unités, résultats, pourcentages et temps de coupe (`from` / `to`, 0,2 s avant la transition) sont dans **`../items.json`** (champ `icp`).
Les unités `skipped` ou `excluded` n'y sont jamais utilisées. Nouveau thème = nouveau dossier.
